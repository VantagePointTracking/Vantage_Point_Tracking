kind = "api"
previewPath = "/api"
title = "API Server"
version = "1.0.0"
id = "3B4_FFSkEVBkAeYMFRJ2e"

[[services]]
localPort = 8080
name = "API Server"
paths = ["/api"]

[services.development]
run = "node server.js"
workdir = "artifacts/api-server"
env = { PORT = "8080" }

[services.production]

[services.production.run]
args = ["node", "artifacts/api-server/server.js"]

[services.production.run.env]
PORT = "8080"
NODE_ENV = "production"
